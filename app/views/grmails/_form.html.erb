<%= form_for(@grmail) do |f| %>
  <% if @grmail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grmail.errors.count, "error") %> prohibited this grmail from being saved:</h2>

      <ul>
      <% @grmail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
